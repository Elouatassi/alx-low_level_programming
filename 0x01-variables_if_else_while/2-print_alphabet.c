#include <stdio.h>

/**
 * main -Etry point
 *
 * Desciption: print all alphabet letters
 *
 * Return: Always 0 (Success)
*/

int main(void)

{
	char A;

	for (A = 'a'; A <= 'z' ; A++)
	putchar(A);
	putchar('\n');
	return (0);
}
